# 基础设施与架构整理

## 目标

将脚手架项目整理为可持续演进的模块化单体，并为未来多 service 预留轻量边界。

## 进度

| 任务 | 状态 |
| --- | --- |
| 新增 `packages/common` | ✅ |
| 抽出 Snowflake / time helper | ✅ |
| 保留 `app/utils` 兼容 wrapper | ✅ |
| 新增 `app/api/router.py` | ✅ |
| 保留 `app/api/api.py` 兼容 wrapper | ✅ |
| 测试目录统一为 `tests/` | ✅ |
| 项目名更改为 `drexor-python` | ✅ |
| Dockerfile 复制 `packages/` | ✅ |
| AGENTS / CLAUDE 中文工程规范 | ✅ |

## 验证

已执行：

```text
make lint
make test
```

当前测试：

- `tests/test_architecture.py`

## 后续

- 删除历史 `test/` 目录残留。
- 进一步整理 `services/api_key` 与 `services/auth` 的边界。
- 为复杂业务引入 UseCase 层时补对应测试。
